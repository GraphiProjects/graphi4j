group = "$group"
version = "$version"

apply plugin: 'java'
apply plugin: 'maven'

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
  mavenLocal()
  mavenCentral()
}

dependencies {
  compile project(':graphi-java') ?: "graphi:graphi-java:$graphiJavaVersion"
}
